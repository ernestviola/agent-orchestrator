YYYY-MM-DD | Short description of change | Diff reviewed: yes/no | Tests passed: yes/no
2026-08-27 | Phase 1: container provisioning layer (src/types.ts, src/roles.ts, src/provisioning.ts) + sandbox/ sub-agent & tinyproxy images + unit and integration tests; housekeeping (package.json name/bin, README, AGENTS.md/DESIGN.md fixes) | Diff reviewed: pending | Tests passed: yes (33 unit, 4 integration)
2026-08-27 | Vertical slice: real model runs one engineer task end to end — src/models.ts (OpenRouter routing), sandbox/agent/agent.mjs (constrained edit/test loop, undici), per-run working-copy + git-diff handoff in LocalResultStore, orq-project.json manifest, fixtures/sample-project/, `--engineer` CLI | Diff reviewed: pending | Tests passed: yes (39 unit; integration 3 pass + 1 end-to-end skipped without OPENROUTER_API_KEY)
2026-08-28 | Fix stale default model: `anthropic/claude-3.5-haiku` was dropped from OpenRouter's catalog (404 "No endpoints found"). Bump DEFAULT_MODEL to `anthropic/claude-haiku-4.5` in src/models.ts; update README.md + docs/CONTEXT.md references | Diff reviewed: pending | Tests passed: yes (39 unit; 4 integration incl. the real-model engineer end-to-end, now green)
2026-08-28 | test-engineer runtime: generalise sandbox/agent/agent.mjs to dispatch on task.role (engineer edit→test loop unchanged; test-engineer authors tests/ once, finishes completed regardless of pass/fail per the human-gate design); LocalResultStore diffs work/{src,tests} + chmods whole working copy (src/provisioning.ts); --test-engineer CLI flag (src/index.ts); AgentResult.diff docstring (src/types.ts); +2 unit tests, +1 skipIf integration e2e | Diff reviewed: pending | Tests passed: yes (41 unit; 5 integration, incl. real-model engineer + test-engineer end-to-end)
2026-08-28 | Orchestrator loop + human approval gate: src/llm.ts (OpenAI-compatible chat client for the trusted tier), src/orchestrator.ts (OrchestratorSession — spin_up_agent tool-use loop, MAX_TOOL_ROUNDS cap, gate enforced in the tool handler: engineer refused until a completed test-engineer run this session is human-approved via the injected approveTests callback), selectOrchestratorModel() in src/models.ts, --orchestrate [projectPath] readline REPL in src/index.ts (deterministic y/n gate prompt). +15 unit tests (tests/orchestrator.test.ts). Manually verified e2e vs fixtures/sample-project: test-engineer → CLI approval → engineer chained in one turn | Diff reviewed: pending | Tests passed: yes (56 unit; integration unchanged at 5)
2026-08-28 | Sandbox dependency support (self-hosting): orq-project.json gains sandboxReadonlyPaths (:ro bind from real target) + sandboxWritablePaths (cp -a into working copy, :rw — Vite/Vitest write node_modules/.vite-temp); assertSafeSandboxPath + resolveTargetHostPath in src/provisioning.ts. Engineer role now mounts tests/ read-only (src/roles.ts + invariant + DESIGN.md) — it couldn't verify its own work otherwise (node --test with no test files exits 0). Root orq-project.json added so --orchestrate . targets this repo. New fixtures/node-runtime-project/. 4 LocalResultStore.finalize tests gated it.skipIf(!HAS_GIT) (sandbox has no git). Verified: --engineer . and --orchestrate . run this repo's vitest suite in-sandbox and complete | Diff reviewed: pending | Tests passed: yes (81 unit; 6 integration)
